<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AlkoLog"
             x:Class="AlkoLog.CatalogPage"
             x:Name="CatalogPageRoot"
             x:DataType="local:CatalogPageViewModel"
             Title="Italkatalógus">

       <Grid RowDefinitions="*,Auto">

              <CollectionView Grid.Row="0"
                                          ItemsSource="{Binding DrinkCatalog}"
                                          SelectedItem="{Binding SelectedItem}"
                                          SelectionMode="Single">
                     <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="local:DrinkCatalogItem">
                                   <Border Stroke="Black"
                                                 StrokeThickness="1"
                                                 StrokeShape="RoundRectangle 20"
                                                 Margin="5"
                                                 BackgroundColor="Transparent">
                                          <Grid ColumnDefinitions="*,Auto"
                                                   Padding="10"
                                                   ColumnSpacing="12">

                                                 <Grid Grid.Column="0"
                                                          ColumnDefinitions="Auto,*"
                                                          ColumnSpacing="12"
                                                          VerticalOptions="Center">
                                                        <Image Source="{Binding ImagePath}"
                                                                  HeightRequest="72"
                                                                  WidthRequest="72"
                                                                  Aspect="AspectFit" />

                                                        <VerticalStackLayout Grid.Column="1"
                                                                                            VerticalOptions="Center">
                                                               <Label Text="{Binding Name}"
                                                                         FontAttributes="Bold"
                                                                         FontSize="18" />
                                                               <Label Text="{Binding AlcoholPercent, StringFormat='Alkoholfok: {0}%'}" />
                                                        </VerticalStackLayout>
                                                 </Grid>

                                          </Grid>
                                   </Border>
                            </DataTemplate>
                     </CollectionView.ItemTemplate>
              </CollectionView>

                    <Grid Grid.Row="1"
                         Padding="10"
                         ColumnSpacing="12"
                         RowSpacing="10"
                         ColumnDefinitions="*,*"
                         RowDefinitions="Auto,Auto">

                          <Button Grid.Row="0"
                                 Grid.Column="0"
                                 Text="Új ital"
                                 Command="{Binding AddNewCommand}" />

                          <Button Grid.Row="0"
                                 Grid.Column="1"
                                 Text="Szerkesztés"
                                 Command="{Binding EditSelectedCommand}" />

                          <Button Grid.Row="1"
                                 Grid.Column="0"
                                 Text="Megivás"
                                 Command="{Binding DrinkSelectedCommand}" />

                          <Button Grid.Row="1"
                                 Grid.Column="1"
                                 Text="Törlés"
                                 Command="{Binding DeleteSelectedCommand}" />

                    </Grid>

       </Grid>

</ContentPage>