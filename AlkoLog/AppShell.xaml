<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AlkoLog.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AlkoLog"
    Shell.FlyoutBehavior="Flyout"
    Title="AlkoLog">

    <FlyoutItem Title="Főoldal">
        <ShellContent
            Title="Főoldal"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Profil">
        <ShellContent
            Title="Profil"
            ContentTemplate="{DataTemplate local:ProfilePage}"
            Route="ProfilePage" />
    </FlyoutItem>

    <FlyoutItem Title="Italkatalógus">
        <ShellContent
            Title="Italkatalógus"
            ContentTemplate="{DataTemplate local:CatalogPage}"
            Route="CatalogPage" />
    </FlyoutItem>

    <FlyoutItem Title="About">
        <ShellContent
            Title="About"
            ContentTemplate="{DataTemplate local:AboutPage}"
            Route="AboutPage" />
    </FlyoutItem>

    <FlyoutItem Title="Alaphelyzet">
        <ShellContent
            Title="Alaphelyzet"
            ContentTemplate="{DataTemplate local:ResetPage}"
            Route="ResetPage" />
    </FlyoutItem>

</Shell>
